{% extends "base.html" %}

{% block content %}

<div class="row">
	<div class="col-md-1"></div>
	<div class="col-md-10">
	<table id="table" class="table table-striped table-bordered table-hover">
		<tr>
  			<td>主任务名</td>
  			<td>子任务名</td>
  			<td>工作内容</td>
  			<td>工作时间</td>
  			<td>工作类型</td>
			<td>操作</td>
		</tr>


		{% for item in projectlist %}
		<tr>
  			<td>{{ item.project }}</td>
  			<td>{{ item.branch }}</td>
  			<td>{{ item.date }}</td>
  			<td>{{ item.worktype }}</td>
  			<td>{{ item.workload }}</td>
			<td>
				<a type="button" href="update.html" class="btn btn-info btn-sm"><span class="glyphicon glyphicon-home"> 编辑</span></a>
        		<a type="button" href="delete.html" class="btn btn-success btn-sm"><span class="glyphicon glyphicon-plus"> 删除</span></a>
			</td>
		</tr>
		{% endfor %}
	</table>
	</div>
<div class="col-md-1"></div>

{% endblock %}
